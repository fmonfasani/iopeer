apiVersion: v1
kind: Service
metadata:
  name: {{ include "iopeer.fullname" . }}
  labels:
    {{- include "iopeer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iopeer.selectorLabels" . | nindent 4 }}
