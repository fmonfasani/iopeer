apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iopeer.fullname" . }}-test-connection"
  labels:
    {{- include "iopeer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iopeer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
